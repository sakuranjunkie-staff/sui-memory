[project]
name = "sui-memory"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "janome>=0.5.0",
    "numpy>=2.4.3",
    "sentence-transformers>=5.3.0",
    "sqlite-vec>=0.1.7",
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
    "pydantic>=2.0.0",
]

[project.scripts]
sui-memory-hook = "src.hook:main"
sui-memory-inject = "src.injector:main"
sui-memory-daemon = "src.daemon:_run"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
